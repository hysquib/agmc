# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name名称: : Deploy Jekyll with GitHub Pages dependencies preinstalled使用 GitHub Pages 部署 Jekyll，并预先安装依赖项

on:触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on:触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on:触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：on：触发条件：:
  # Runs on pushes targeting the default branch# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行# 仅在推送到默认分支时运行# 仅在推送到默认分支时运行  # 仅在推送到默认分支时运行
  push:  push 事件：:推送事件：: 推送:  ush 事件：:推送事件：:推送事件：:推送事件：:
    branches    分支: ["main"]: ["main"]    分支: ["main"]: ["main"]

  # Allows you to run this workflow manually from the Actions tab  # 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流  # 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流  # 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流# 允许您从“Actions”选项卡手动触发此工作流
  workflow_dispatch:  workflow_dispatch 事件：: w工作流调度事件：: w工作流调度事件：: workflow_dispatch:  w工作流调度:  w工作流调度:  workflow_dispatch 事件：:工作流调度事件：:工作流调度事件：: workflow_dispatch:  w工作流调度:  workflow_dispatch 事件：: w工作流调度事件：: w工作流调度事件：: workflow_dispatch:  w工作流调度:  w工作流调度:  workflow_dispatch 事件：:工作流调度事件：: workflow_dispatch:   workflow_dispatch:  workflow_dispatch 事件：: w工作流调度事件：: w工作流调度事件：: workflow_dispatch:  w工作流调度:  w工作流调度:  workflow_dispatch 事件：:工作流调度事件：:工作流调度事件：: workflow_dispatch:  w工作流调度:  workflow_dispatch 事件：: w工作流调度事件：: w工作流调度事件：: workflow_dispatch:  w工作流调度:  w工作流调度:  workflow_dispatch 事件：:工作流调度事件：: workflow_dispatch:  workflow_dispatch 事件：: w工作流调度事件：: w工作流调度事件：: workflow_dispatch:  w工作流调度:  w工作流调度:  workflow_dispatch 事件：:工作流调度事件：:工作流调度事件：: workflow_dispatch:  w工作流调度:  workflow_dispatch 事件：: w工作流调度事件：: w工作流调度事件：: workflow_dispatch:  w工作流调度:  w工作流调度:  workflow_dispatch 事件：:工作流调度事件：:工作流调度事件：:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages
permissions:权限:
  contents: read内容: 读取
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。# 仅允许一次并发部署，跳过在当前运行与最新排队之间发生的排队任务。
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。# 但是，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。不过，请勿取消正在进行中的运行，因为我们希望这些生产部署能够顺利完成。
concurrency:并发：:
  group组: "pages"“页面”: "pages"“页面”  组: "pages"“页面”: "pages"“页面”
  cancel-in-progress: false正在取消: 假

jobs:作业:
  # Build job  # 构建作业
  build:  构建:
    runs-on: ubuntu-latest    运行环境: ubuntu-latest
    steps:    步骤:
      - name名称: Checkout
        uses: actions/checkout@v4
      - name名称: Setup Pages
        uses: actions/configure-pages@v5
      - name名称:       - 名称: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
